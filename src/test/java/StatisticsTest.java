import console_bot.Bot;import console_bot.ResourceStorage;import org.junit.Test;import utils.MyUtils;import java.io.ByteArrayOutputStream;import java.io.PrintStream;import static org.junit.Assert.assertEquals;import static org.junit.Assert.assertThat;public class StatisticsTest {    @Test    public void testOmegaLol() {        final ResourceStorage storage = new ResourceStorage();        final Bot bot = new Bot(storage);        MyUtils.isRandomFixed = true;        //теперь getRandom всегда выдает минимальное значение        //то есть всегда выводится первый вопрос и первые случайные реплики        bot.printer.consoleMode = false;        bot.input.consoleMode = false;        new Thread(bot).start();        bot.input.addToQueue("js"); //дать команду в очередь        System.out.println(bot.printer.que.remove()); //достать из очереди вывод программы        bot.input.addToQueue("nothing");        System.out.println(bot.printer.que.remove());        bot.input.addToQueue("q");        System.out.println(bot.printer.que.remove());        System.out.println(bot.printer.que.remove());        System.out.println(bot.printer.que.remove());    }}