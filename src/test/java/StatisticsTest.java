import org.junit.Test;import utils.MyUtils;import java.io.ByteArrayOutputStream;import java.io.PrintStream;import static org.junit.Assert.assertEquals;import static org.junit.Assert.assertThat;public class StatisticsTest {    @Test    public void testOmegaLol() {        final PrintStream standardOut = System.out;        final ByteArrayOutputStream outputStreamCaptor = new ByteArrayOutputStream();        System.setOut(new PrintStream(outputStreamCaptor));     //теперь вывод перенаправляется в outputStreamCaptor        final ResourceStorage storage = new ResourceStorage();        final Bot bot = new Bot(storage);        MyUtils.isRandomFixed = true;        //теперь getRandom всегда выдает минимальное значение        //то есть всегда выводится первый вопрос и первые случайные реплики        bot.input.addToQueue("js", "q", "11", "11", "11", "11", "11", "11", "1", "qwe", "Asd", "tyu",                "back");        //заранее определяется ввод        bot.run();        bot.run();        bot.run();        bot.run();        bot.run();        bot.run();        String[] a = outputStreamCaptor.toString().split("\n");        int len = a.length;        assertEquals("Переходим к изучению JavaScript!\r" +                "Вводи question для получения вопроса, ну или help, для всякого другого\r", a[0] + "\r" + a[1]);        assertEquals("'help' - главное лекарство от незнания\r", a[len-4]);        assertEquals("'help' - главное лекарство от незнания\r", a[len-3]);        assertEquals("'help' - главное лекарство от незнания\r", a[len-2]);        assertEquals("Назад в меню!\r", a[len-1]);    }}